<div class="flex flex-col pb-6">
    <dt class="mb-1 text-gray-500 md:text-lg dark:text-gray-400">{{ title }}</dt>
    <dd class="text-lg font-semibold {{ class|default('') }}">{% block content %}{% endblock %}</dd>
</div>
